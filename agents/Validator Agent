from agents.base_agent import BaseAgent


class ValidatorAgent(BaseAgent):
    def run(self, input_data: dict) -> dict:
        """
        Validates facts and sources.
        """
        return {
            "validated": True,
            "confidence_score": 0.0
        }
